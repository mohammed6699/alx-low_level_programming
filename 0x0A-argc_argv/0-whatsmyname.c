# include "main.h"
# include <stdio.h>
/**
 * main - main function - that prints its name, followed by a new line
 * @argc: argument count (void)
 * @argv: argument vector
 * Description: program that prints its name, followed by a new line.
 * Return: o
 */
int main(int argc, char *argv[])
{
(void)argc;
printf("%s\n", argv[0]);
return (0);
}
