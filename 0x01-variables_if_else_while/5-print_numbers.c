# include <stdio.h>
/**
 * main - print decimal
 *
 * The basics of main function
 *
 * Return: always 0 (success)
 */
int main(void)
{
char ch;
for (ch = '0'; ch <= '9'; ch++)
putchar (ch);
putchar('\n');
return (0);
}
